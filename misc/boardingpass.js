module.exports.run = async (bot, message, args, extras) => {
    
}

module.exports.config = {
    name: "boardingpass",
    usage: "",
    description: "DM's user boarding pass on verification",
    accessibleby: "private"
}